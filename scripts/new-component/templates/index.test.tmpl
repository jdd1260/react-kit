import React from 'react';
import { shallow } from 'enzyme';

import { {{ component }} } from '../{{ directory }}';

describe('{{ component }}', () => {
  describe('render', () => {
    it('does not crash', () => {
      shallow(<{{ component }} />);
    });
  });
});

